{
  "schemaVersion": 1,
  "name": "chrome-background",
  "displayName": "Chrome",
  "definitionVersion": 3,
  "updateDate": "2026-08-12T00:00:00Z",
  "description": "Stop Google Chrome from running in the background.",
  "actions": [
    {
      "id": "kill-chrome",
      "type": "killProcess",
      "match": { "name": "chrome.exe" },
      "killTree": true,
      "backgroundOnly": true,
      "comment": "Stop Chrome only when its process tree has no visible browser window"
    },
    {
      "id": "kill-elevation-service",
      "type": "killProcess",
      "match": { "name": "elevation_service.exe", "company": "Google LLC" },
      "optional": true,
      "comment": "Stop Chrome's elevation helper (the process stop cannot be undone automatically)"
    },
    {
      "id": "set-background-mode",
      "type": "setRegistryValue",
      "hive": "HKLM",
      "path": "SOFTWARE\\Policies\\Google\\Chrome",
      "name": "BackgroundModeEnabled",
      "valueType": "DWord",
      "data": 0,
      "comment": "Policy: Chrome must NOT keep background apps alive after the window is closed"
    },
    {
      "id": "remove-chrome-autolaunch",
      "type": "removeAutostart",
      "kind": "registryRun",
      "scope": "allUsers",
      "name": "GoogleChromeAutoLaunch_*",
      "comment": "Remove Chrome's per-user auto-launch on logon (name has a per-install hash suffix)"
    },
    {
      "id": "stop-gupdate",
      "type": "stopService",
      "name": "gupdate",
      "disable": true,
      "optional": true,
      "comment": "Google Update service (optional; disabling pauses Chrome auto-updates)"
    },
    {
      "id": "stop-gupdatem",
      "type": "stopService",
      "name": "gupdatem",
      "disable": true,
      "optional": true,
      "comment": "Google Update per-machine service (optional)"
    },
    {
      "id": "disable-google-update-task",
      "type": "removeAutostart",
      "kind": "scheduledTask",
      "taskPath": "\\",
      "name": "GoogleUpdateTaskMachine*",
      "disableOnly": true,
      "optional": true,
      "comment": "Disable (not delete) the Google updater tasks that re-spawn background processes"
    }
  ]
}

